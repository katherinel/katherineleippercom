.proj_box
	= image_tag(Image.find(p.default_image_id).url(:small), :class => 'proj_image')
	.proj_years
		- for year in p.years
			%p.year= year
	.proj_title{:style => "#{proj_title_bkgd(p)}"}
		%h3= link_to "#{p.title}", project_path(p)